# 工作流与项目现况（2026-08-21）

## 项目现况

- **正式开发主线 = Godot 4.7 客户端**（`godot/`，主场景 `channel_3d.tscn`）。玩法规则、运行数据、交互、3D 表现与回归测试都在这里落地。
- **Web（`web/`）**：低成本玩法验证原型，以共享盘 `CabinSlice_织梦频道.exe` 的实际行为为规则基准，不承担正式 UI/发布。
- **Unity 旧构建**：仓库外层目录的 `DreamChannel.exe`（2025-08 构建）与仓库隔离，不要运行/修改。
- **仓库以 git 为准**：两台开发机各自 clone，绝对路径不同（如 `G:\dream-channel` 与 `C:\Users\Admin\Documents\channel_dream\dream-channel`），文档一律使用仓库相对路径。
- 团队共享盘：`\\192.168.1.21\bytedance\Shared\new channel`。

## 本会话（2026-08-21）完成的改动

1. **莉莉黏土角色**：FBX（41 骨骼 + 完整动作集）替换临时 Quaternius 模型，manifest 动画映射含坐/休息/工作互动。
2. **镜头系统**（探索 + 战斗共用，`camera_follow_math.gd` 共享数学）：
   - 开局由远至进入场运镜
   - 玩家移动延迟跟随（只平移不旋转，旋转由玩家选择）
   - 玩家偏下构图（探索 0.15 / 战斗偏上对准 0.12）
   - 拖动松手延迟 1.5s 回位（保持玩家选择的旋转与缩放）
   - 战斗镜头自始至终对准玩家-怪物中点偏上，跟随速率与探索一致（3.4）
3. **战斗 UI 杀戮尖塔式**：棋盘全幅无框无蒙层、手牌底部露出悬停升起、敌人意图 3D 头顶 Label3D、状态条悬浮顶角。
4. **UI 修复**：phase 切换不同步 world rect 的点击错位根因修复；跟随期间镜头跳变修复。
5. **测试**：新增 `camera_dolly_follow_regression`、`ui_hit_regression`，更新 4 个既有测试；全套 headless 回归通过（2 个依赖 `user://` 写入的测试在受限沙箱环境失败，属环境限制）。
6. **审美打磨提示词**：`docs/battle_ui_polish_prompt.md`，可交给 Codex 做视觉打磨（不改行为）。

## 工作流

1. **修改**：只改 `godot/`（代码/场景/数据）与 `docs/`；`.godot/`、`artifacts/`、日志不提交。
2. **自检**：`<godot> --headless --path "<repo>\godot" --script res://tests/<test>.gd`（Godot 4.7.1；测试清单见 `godot/README.md` 自检节）。
3. **提交**：`feat/fix/docs(godot):` 前缀；推送走 `gh` CLI 凭证（本机 `gh auth` 已登录 `psqd12137-sudo`）。
4. **AI 分工**：功能/逻辑/测试在 DSH 完成并推送；视觉审美细化可写提示词交给 Codex（godot-mcp 连接运行中的 Godot 截图自查）。
5. **验收**：功能行为以 headless 回归测试为底线；视觉以实际运行 `channel_3d.tscn`（F5）为准。

## 下一步（待办）

- 战斗 UI 视觉打磨（提示词已备：`docs/battle_ui_polish_prompt.md`，交给 Codex）
- 玩家验收：入场运镜/跟随/回位/偏下构图的手感参数（速率 3.4、回位延迟 1.5s、偏移 0.15/0.12）
- 待确认：`apps/`、`cabin-slice/`、`godot-mcp-pro-v1.16.0/` 三个未跟踪目录是否入库或清理
- 建议：`gh auth refresh` 轮换一次 gh OAuth token（早期调试曾把 token 打印进会话日志）
